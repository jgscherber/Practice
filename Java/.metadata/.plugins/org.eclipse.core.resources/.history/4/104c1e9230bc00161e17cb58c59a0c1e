import java.util.Scanner;

public class nameBreak {
	public static void main(String[] args){
		Scanner stdin = new Scanner(System.in);
		System.out.print("Enter your full name: ");
		String full_name = stdin.next();
		stdin.close();
		int spLoc = full_name.indexOf(" ");
		String first_name = full_name.substring(0, spLoc);
		String last_name = full_name.substring(spLoc,-1);
		System.out.print("Your first name is "+first_name);
		System.out.print("Your last name is "+last_name);
		
	}
}
