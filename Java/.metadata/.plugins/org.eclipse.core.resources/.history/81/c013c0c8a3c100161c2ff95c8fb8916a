
public class Exercise_31 {

	public static void main(String[] args) {
		int dice1;
		int dice2;
		int[] rolls; // creates a variable of type int-array 
		rolls = new int[100]; // creates a new int-array of size 100 and assigns it to rolls
		boolean snakes = false;
		int trials = 0;
		int roll_num = 1;
		while((trials != 100))
		{
			while (!snakes)
			{
			dice1 = (int)(Math.random()*5)+1;
			dice2 = (int)(Math.random()*5)+1;
			if (dice1+dice2== 2)
			{
				snakes = true;
				
			}
			else
			{
				roll_num += 1;
			}
			}
			rolls[trials] = roll_num;
		}
		for(int i=0;i<100;i++)
		{
			
		}
		

	}

}
